import react from 'react';
import styled from 'styled-components';

const Hr = styled.hr`
    border: 1px solid #282828;
    width: 90%;
    margin: 10px auto;
    padding: 0;
`;

const Line = () => {
    return(
        <Hr/>
    );
}

export default Line;